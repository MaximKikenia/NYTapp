import { useState, useEffect, useRef } from "react";
import { useDispatch, useSelector } from "react-redux";

function PopularBooks() {
    return (
        <div>
            <p>Здесь будет второе приложение. Популярные книги АPI.</p>
        </div>
    );
};

export {PopularBooks};